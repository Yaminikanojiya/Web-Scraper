# Web-Scraper
Secure Flask Web Scraper API — Extracts titles, quotes, images, or custom elements from webpages with built-in SSRF protection, content-size limits, and CSS selector support. Fast, safe, and returns clean JSON output.
